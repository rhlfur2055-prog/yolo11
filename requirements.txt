# YOLO11 한국 번호판 인식 — 최소 재현 의존성
# Python 3.10+ 권장
# GPU: torch / paddlepaddle 은 환경에 따라 별도 휠 설치 권장
#   - torch:  https://pytorch.org/get-started/locally/
#   - paddle: pip install paddlepaddle-gpu  (CUDA 환경)

# ── Core: YOLO 탐지 + 영상 I/O ──
ultralytics>=8.3,<9.0
opencv-python>=4.8
numpy
Pillow

# ── Deep learning runtime (CRNN + YOLO + Paddle) ──
torch>=2.0,<3.0
torchvision>=0.15

# ── OCR: PaddleOCR (한국어 특화) ──
paddlepaddle>=3.0
paddleocr>=3.0,<4.0
pytesseract                 # plate_recognition_4k 보조 OCR 백엔드

# ── Detection helpers ──
sahi>=0.11                  # plate_recognition_4k 슬라이스 추론
huggingface_hub             # CRNN 체크포인트 다운로드

# ── REST API 서버 (plate_server.py) ──
fastapi
uvicorn[standard]

# ── YouTube 다운로드 헬퍼 (youtube_helper.py) ──
yt-dlp
