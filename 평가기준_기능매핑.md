# 평가 기준 ↔ 구현 기능 매핑

**과목:** 정규교과 / 교통안전을 위한 AI 솔루션  
**과제:** 이동하는 차량 파일을 입력받아 차량번호를 인지하여 표시하는 AI 프로그램

아래 평가 항목별로 **어디에 해당 기능이 들어갔는지** 코드 위치를 정리했습니다.

---

## 1. 자기평가서 (40점)

- 제출용 자기평가서에 본 프로젝트에서 구현한 기능을 체크·기재하면 됩니다.
- 아래 2~6번 항목이 코드에 반영되어 있음을 이 문서와 소스 주석으로 확인할 수 있습니다.

---

## 2. 라벨링 작업 여부 (5점)

- **의미:** 차량/번호판 이미지를 업로드하여 라벨링을 수행했는지.
- **구현:** Roboflow 등으로 번호판 영역을 라벨링한 데이터를 사용합니다.
- **코드 연계:** 라벨링 결과는 YOLO 형식으로 `dataset/yolo_format/` 또는 `dataset/` 에 두고, `train.py` 학습 시 사용합니다.
- **관련 파일:** `train.py`, `dataset/yolo_format/data.yaml`, `auto_label.py`(선택)

---

## 3. 데이터 셋 생성 여부 (5점)

- **의미:** 라벨링된 자료로 train/val/test 등 데이터셋을 구성했는지.
- **구현:** `dataset/yolo_format/data.yaml` 에 경로·클래스 설정. `train.py`에서 해당 yaml을 읽어 학습합니다.
- **관련 파일:** `dataset/yolo_format/data.yaml`, `train.py` (DATA_YAML)

---

## 4. 입력화일에 차량에 detect 표시 여부 (10점)

- **의미:** 입력 영상/이미지에서 차량(번호판) 영역을 탐지하고 화면에 표시하는지.
- **구현:** YOLO로 번호판 bbox 탐지 → 프레임에 사각형(bbox) 그리기.
- **코드 위치:**
  - **plate_server.py**  
    - 실시간 스트림에서 결과를 그리는 부분:  
      `# [평가기준 10점] 입력화일에 차량(번호판) detect 표시`  
      → `cv2.rectangle(frame, (x1, y1), (x2, y2), ...)` 로 bbox 표시.
  - **plate_engine_pro.py**  
    - `process_frame()`: `detections = self.model(frame, ...)` 로 탐지 후 `results[].bbox` 반환.  
    - **plate_engine_pro.py** `process_video()`: `cv2.rectangle(frame, (x1, y1), (x2, y2), ...)` 로 동영상에 표시.

---

## 5. ROI, CROP, CLAHE, Homography 함수 여부 (20점)

- **의미:** ROI(관심 영역), CROP(잘라내기), CLAHE(대비 향상), Homography(원근 보정)를 사용했는지.

| 기능 | 구현 위치 | 설명 |
|------|-----------|------|
| **ROI** | `plate_engine_pro.py` → `process_frame()` | YOLO bbox 기준으로 `rx1, ry1, rx2, ry2` 로 관심 영역 좌표 설정. 주석: `[평가기준 20점] ROI (Region of Interest) + CROP` |
| **CROP** | 같은 위치 | `roi = crop_src[ry1:ry2, rx1:rx2]` 로 번호판 영역 이미지 추출. |
| **CLAHE** | `plate_engine_pro.py` → `ImagePreprocessor.clahe()` | LAB 채널에 CLAHE 적용. docstring: `[평가기준 20점] CLAHE ...` |
| **Homography** | `exam_realtime_plate.py` → `apply_homography()` | `getPerspectiveTransform` + `warpPerspective` 로 원근 보정. 시험용 채점 항목 8번(5점) 명시. |

- **추가:**  
  - `plate_engine_pro` 전처리 루프에서 `PREPROCESS_METHODS`에 `"clahe"` 포함되어 CLAHE가 적용됩니다.  
  - 기울기 보정은 `ImagePreprocessor.deskew()` (warpAffine) 사용.  
  - 원근 보정(Homography)은 `exam_realtime_plate.py`의 `apply_homography()`에서 명시적으로 구현되어 있습니다.

---

## 6. 차량번호 인식 표시 여부 (20점)

- **의미:** 인식된 차량 번호판 문자열을 화면(또는 결과)에 표시하는지.
- **구현:** OCR 앙상블 → 검증 후 `plate`, `confidence` 반환 → 화면에 텍스트로 표시.
- **코드 위치:**
  - **plate_engine_pro.py** → `process_frame()`  
    - `# [평가기준 20점] 차량번호 인식 표시`  
    - `results.append({"plate": best_text, "confidence": best_conf, "bbox": ...})` 로 인식 결과 반환.
  - **plate_server.py** (실시간 스트림)  
    - `# [평가기준 20점] 차량번호 인식 표시`  
    - `label = r.get("plate") or r.get("text")` 후 `_put_kr_text(frame, label, ...)` 로 화면에 번호 표시.
  - **plate_engine_pro.py** → `process_video()`  
    - `label = f"{r['plate']} ({r['confidence']:.0%})"` 후 `cv2.putText(...)` 로 영상에 표시.

---

## 요약: best.pt 및 외부 파일 인지

- **best.pt 생성:**  
  - Roboflow 등으로 라벨링·데이터셋 생성 → `train.py`로 YOLO 학습 → `runs/detect/highway_plate/weights/best.pt` 생성.  
  - 평가 기준의 “yolo를 이용 학습자료를 학습시켜 best.pt 모델을 생성”에 대응합니다.
- **외부 파일 차량 객체 인지:**  
  - `plate_server.py`(영상/웹캠), `plate_engine_pro.process_video()`(동영상 파일), `plate_gui.py`(동영상) 등에서 **외부 입력 파일**을 받아 `PlateEnginePro`(best.pt 또는 우선순위 모델)로 번호판을 인지하고, 위 4·6번처럼 detect 표시 및 차량번호 인식 표시를 수행합니다.

제출 시 위 파일명과 주석(`[평가기준 ...점]`)을 캡처해 보여주면, 각 항목이 구현되었음을 쉽게 설명할 수 있습니다.
